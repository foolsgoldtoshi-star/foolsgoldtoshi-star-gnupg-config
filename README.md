# GPG Configuration for foolsgoldtoshi-star

This repository manages GPG configuration files via symlinks to keep them synchronized across setups.

## Files:
- gpg-agent.conf: GPG agent configuration
- common.conf: Common GPG settings

## Usage:
These files should be symlinked to ~/.gnupg/ for active use.
